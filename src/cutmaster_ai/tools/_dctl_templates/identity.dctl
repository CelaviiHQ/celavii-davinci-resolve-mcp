// Identity DCTL — passes pixels through unchanged. Baseline scaffold.
// CutMaster AI · v5 Wave 3 · template id: identity

DEFINE_UI_PARAMS({{UI_PARAMS}})

__DEVICE__ float3 transform(int p_Width, int p_Height, int p_X, int p_Y, float p_R, float p_G, float p_B)
{
    return make_float3(p_R, p_G, p_B);
}
